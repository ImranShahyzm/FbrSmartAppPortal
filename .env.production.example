# Copy to `.env.production` before `npm run build`.
#
# Single-site deploy (Vite build copied into API wwwroot, one HTTPS URL for UI + /api):
#   Leave empty so the browser calls `/api` on the same host.
VITE_API_BASE_URL=http://103.86.135.181:2110

# Split deploy (static site on another origin, API on its own URL):
# VITE_API_BASE_URL=https://api.yourcompany.com
